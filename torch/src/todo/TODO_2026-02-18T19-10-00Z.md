# TODO: InnerHTML Migration for `landing/index.html`

## Assignments to Migrate

1.  **Line ~777**: `contentDiv.innerHTML = marked.parse(text);`
    - **Context**: Renders markdown documentation from `../src/docs/TORCH.md`.
    - **Source**: Local file content (trusted source, but markdown parsing can yield HTML).
    - **Type**: Template/Generated HTML.
    - **Status**: Investigate if `marked` can be used safely without `innerHTML` (unlikely without sanitizer) or if we should document this exception. Or better: use `setHTML` (if available/polyfilled) or sanitization. But we can't add libraries.
    - **Proposal**: Document as trusted source exception or find a way to escape if needed.

2.  **Line ~781**: `contentDiv.innerHTML = \`<p class="text-danger">Error loading documentation: ${error.message}</p>\`;`
    - **Context**: Displaying error message.
    - **Source**: `error.message`.
    - **Type**: Template with interpolation.
    - **Status**: Migrate to `createElement` + `textContent` or `escapeHtml`.

3.  **Line ~834**: `btn.innerHTML = originalContent;`
    - **Context**: Restoring button content after "Copied!" message.
    - **Source**: `btn.innerHTML` captured earlier.
    - **Type**: HTML String from DOM.
    - **Status**: Migrate to capturing `childNodes` and using `replaceChildren()`.

## Reads (Reference)
- `const originalContent = btn.innerHTML;` (Line ~829) - Will be replaced by node cloning.
