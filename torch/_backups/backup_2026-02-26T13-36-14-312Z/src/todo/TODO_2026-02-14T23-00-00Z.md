# TODO: dashboard/index.html innerHTML Migration

- [ ] `summaryBar.innerHTML = cards.map(...).join('')` (Line ~436)
  - Strategy: Replace with `document.createElement` loop and `replaceChildren`.
- [ ] `lockGrid.innerHTML = ''` (Line ~448)
  - Strategy: `lockGrid.replaceChildren()` + `appendChild(emptyState)`.
- [ ] `lockGrid.innerHTML = locks.map(...).join('')` (Line ~455)
  - Strategy: Create `createLockCard` function using `document.createElement`, then `lockGrid.replaceChildren(...cards)`.
- [ ] `rawLog.innerHTML = ''` (Line ~534)
  - Strategy: `rawLog.replaceChildren()`.
