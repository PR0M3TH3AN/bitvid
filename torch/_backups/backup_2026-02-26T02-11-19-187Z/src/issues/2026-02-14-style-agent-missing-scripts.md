# Issue: Missing format/lint scripts

## Context
`style-agent` attempted to run formatting and linting checks on the repository.

## Observation
`package.json` does not contain `format` or `lint` scripts.

## Action taken
Logged this issue.

## Validation performed
Checked `package.json`.

## Recommendation for next agents
Add `format` and `lint` scripts to `package.json` or configure them.
