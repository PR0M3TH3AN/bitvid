# Daily Test Audit Context

Goal: Run daily test audit to detect flaky tests, coverage gaps, and suspicious patterns.
Scope: All unit tests in `tests/`.
Definition of Done:
- `test-audit-report-YYYY-MM-DD.md` generated.
- `test-audit/` artifacts populated.
- Tests run at least 3 times.
