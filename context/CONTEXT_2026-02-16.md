# Context: Constants Refactor (2026-02-16)

## Goal
Discover and replace duplicated numeric constants in `js/` with canonical named constants.
Target branch: `unstable`

## Scope
- Identify duplicated numeric literals.
- Prioritize semantic matches (timeouts, etc.).
- Replace with imports from `js/constants.js` or `js/nostr/nip07Permissions.js`.
- Verify with lint and tests.

## Changes
1. `js/ui/videoModalController.js`: Replaced `5000` with `SHORT_TIMEOUT_MS`.
2. `js/subscriptions.js`: Replaced `5000` with `NIP07_EXTENSION_WAIT_TIMEOUT_MS`.
