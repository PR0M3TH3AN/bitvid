# Test Audit Context - 2025-02-18

## Mission
To audit the test suite for behavior fidelity, determinism, and cheat vectors. The goal is to build a daily test-audit workflow that identifies flaky tests, suspicious patterns, and coverage gaps in critical modules.

## Scope
- Unit tests (`tests/` excluding `visual/` and `e2e/`).
- Critical modules:
  - `js/services/authService.js`
  - `js/relayManager.js`
  - `js/nostr/dmDecryptWorker.js`
  - `js/nostr/watchHistory.js`
  - `js/userBlocks.js`

## Definition of Done
- `test-audit-report-YYYY-MM-DD.md` generated.
- `test-audit/flakiness-matrix.json` generated.
- `test-audit/suspicious-tests.json` generated.
- `test-audit/coverage-gaps.json` generated.
- Prioritized remediation tasks identified.
