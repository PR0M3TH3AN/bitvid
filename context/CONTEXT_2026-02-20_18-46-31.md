# Documentation Plan for js/services/playbackService.js

**File:** `js/services/playbackService.js`
**Lines:** ~650
**Reason:** Critical component for "Hybrid Playback Strategy", complex state machine (`PlaybackSession`), and fallback logic. Contains long methods like `execute()` that need flow documentation.

**Plan:**
1.  **Analyze**: Understand `PlaybackService` and `PlaybackSession` interactions.
2.  **Document**:
    *   Add JSDoc to `PlaybackService` methods (`probeHostedUrl`, `registerUrlPlaybackWatchdogs`, etc.).
    *   Add JSDoc to `PlaybackSession` methods (`start`, `execute`).
    *   Add flow comments to `execute()` explaining the fallback sequence.
3.  **Overview**: Create `docs/playbackService-overview.md` with sequence diagrams (text) and usage examples.
4.  **Verify**: Run `npm run lint` and `npm run test:unit`.
